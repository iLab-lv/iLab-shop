import 'server-only'; import { NextResponse } from 'next/server'; import { ProductInputError } from './adminProductValidation.mjs'; import { PRIVATE_NO_STORE } from './auth/adminMutationAuth';
export function productErrorResponse(error, fallback) { if (error instanceof ProductInputError) return NextResponse.json({ error: error.message }, { status: error.status, headers: PRIVATE_NO_STORE }); console.error(fallback, error); return NextResponse.json({ error: fallback }, { status: 500, headers: PRIVATE_NO_STORE }); }
