import { NextResponse } from 'next/server'; import { deleteAdminProduct, updateAdminProduct } from '../../../../../lib/adminProducts'; import { PERMISSIONS } from '../../../../../lib/auth/roles.mjs'; import { authorizeAdminMutation, PRIVATE_NO_STORE } from '../../../../../lib/auth/adminMutationAuth'; import { readJsonObject } from '../../../../../lib/adminCatalogHttp'; import { productErrorResponse } from '../../../../../lib/adminProductHttp';
export async function PATCH(request, { params }) { const denied = await authorizeAdminMutation(request, PERMISSIONS.MANAGE_CATALOG); if (denied) return denied; try { const { id } = await params; return NextResponse.json({ product: await updateAdminProduct(id, await readJsonObject(request)) }, { headers: PRIVATE_NO_STORE }); } catch (error) { return productErrorResponse(error, 'Unable to update product.'); } }
export async function DELETE(request, { params }) { const denied = await authorizeAdminMutation(request, PERMISSIONS.MANAGE_CATALOG); if (denied) return denied; try { const { id } = await params; await deleteAdminProduct(id); return NextResponse.json({ ok: true }, { headers: PRIVATE_NO_STORE }); } catch (error) { return productErrorResponse(error, 'Unable to delete product.'); } }
